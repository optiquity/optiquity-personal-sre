{
  "//": "STANDARD preset (guide/10-permissions.md). Reads + scratch writes under /tmp; every real write, all other shell, network, installs, VC and material actions prompt. Paths: `//tmp/**` is the absolute /tmp — a single leading `/` would anchor at the settings file's own directory. Read-only shell commands run via Claude Code's built-in read-only set (see cautious). The default day-to-day posture.",
  "permissions": {
    "allow": [
      "Read",
      "Write(//tmp/**)",
      "Edit(//tmp/**)"
    ],
    "deny": [
      "Read(./**/.env)",
      "Read(./**/.env.*)",
      "Read(./**/*.key)",
      "Read(./**/*.pem)",
      "Read(./**/id_ed25519*)",
      "Read(./**/id_rsa*)",
      "Read(./**/*.kdbx)"
    ]
  },
  "//stillPrompts": "git add/commit/push and all VC mutations, any non-listed shell command, network/installs, service bootstraps, deletes, and every material/outward-facing action. These NEVER auto-run in any preset."
}
